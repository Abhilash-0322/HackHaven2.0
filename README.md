# ZenHeaven Vaultline

Vault strategy allocator. React + Vite + Tailwind frontend on port 6062.

Routes: `/` `/login` `/register` `/dashboard` `/chat` `/journal` `/books` `/music` `/therapists` `/coins`.
