# ZenHeaven Drift

Synthetic asset platform. React + Vite + Tailwind frontend on port 6068.

Routes: `/` `/login` `/register` `/dashboard` `/chat` `/journal` `/books` `/music` `/therapists` `/coins`.
