# ZenHeaven Flash

Flash loan arbitrage studio. React + Vite + Tailwind frontend on port 6061.

Routes: `/` `/login` `/register` `/dashboard` `/chat` `/journal` `/books` `/music` `/therapists` `/coins`.
