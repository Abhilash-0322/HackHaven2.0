# ZenHeaven Routewise

DEX aggregator routing. React + Vite + Tailwind frontend on port 6065.

Routes: `/` `/login` `/register` `/dashboard` `/chat` `/journal` `/books` `/music` `/therapists` `/coins`.
