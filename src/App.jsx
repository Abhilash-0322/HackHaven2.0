import { useEffect, useMemo, useState } from 'react'
import { Link, NavLink, Navigate, Route, Routes, useLocation, useNavigate } from 'react-router-dom'
import {
  ArrowRight, BookHeart, BookOpen, Check, CircleHelp, Coins, Headphones,
  Heart, Leaf, Lightbulb, LoaderCircle, LockKeyhole, LogOut, Menu, MessageCircle, Music2,
  NotebookPen, Play, Plus, Search, Send, ShieldCheck, Sparkles, Star, Sun, Users, X, Zap,
} from 'lucide-react'
import { auth, books, chat, coins, getStoredUser, journal, music, therapists } from './lib/api'

const cx = (...classes) => classes.filter(Boolean).join(' ')

function Logo({ light = false }) {
  return <Link to="/" className={cx('flex items-center gap-2 font-extrabold tracking-tight', light ? 'text-white' : 'text-ink')}>
    <span className={cx('grid h-9 w-9 place-items-center rounded-xl', light ? 'bg-white/15 text-white' : 'bg-primary text-white')}><Leaf size={19} strokeWidth={2.7} /></span>
    <span className="text-lg">zen<span className={light ? 'text-sky-200' : 'text-primary'}>heaven</span></span>
  </Link>
}

function Button({ children, variant = 'primary', className = '', ...props }) {
  return <button className={cx('inline-flex items-center justify-center gap-2 rounded-full px-5 py-3 text-sm font-bold transition hover:-translate-y-0.5 disabled:cursor-not-allowed disabled:opacity-60', {
    'bg-primary text-white shadow-lg shadow-primary/20 hover:bg-primary-dark': variant === 'primary',
    'border border-slate-200 bg-white text-ink hover:border-primary/30 hover:bg-sky': variant === 'white',
    'bg-ink text-white hover:bg-primary': variant === 'dark',
    'bg-transparent text-primary hover:bg-sky': variant === 'ghost',
    'border border-white/30 bg-white/10 text-white hover:bg-white/20': variant === 'light',
  }, className)} {...props}>{children}</button>
}

function Card({ children, className = '' }) {
  return <div className={cx('rounded-3xl border border-slate-100 bg-white shadow-card', className)}>{children}</div>
}

function AppShell({ children }) {
  const location = useLocation()
  const navigate = useNavigate()
  const [open, setOpen] = useState(false)
  const user = getStoredUser()
  const links = [
    { to: '/dashboard', label: 'Overview', icon: Sun },
    { to: '/chat', label: 'CalmBot', icon: MessageCircle },
    { to: '/journal', label: 'Journal', icon: NotebookPen },
    { to: '/books', label: 'Books', icon: BookOpen },
    { to: '/music', label: 'Music', icon: Music2 },
    { to: '/therapists', label: 'Therapists', icon: Heart },
    { to: '/coins', label: 'Calm coins', icon: Coins },
  ]
  return <div className="min-h-screen bg-[#f7fbff]">
    <aside className={cx('fixed inset-y-0 left-0 z-30 flex w-72 flex-col border-r border-slate-100 bg-white px-5 py-6 transition-transform lg:translate-x-0', open ? 'translate-x-0' : '-translate-x-full')}>
      <div className="flex items-center justify-between px-2"><Logo /><button className="rounded-full p-2 text-slate-400 lg:hidden" onClick={() => setOpen(false)}><X size={20} /></button></div>
      <div className="mt-10 rounded-2xl bg-sky px-4 py-4">
        <div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-primary text-sm font-bold text-white">{(user?.full_name || user?.username || 'A')[0]}</div><div><p className="text-xs text-slate-500">Good to see you</p><p className="font-bold text-ink">{user?.full_name?.split(' ')[0] || 'friend'} <span className="text-lg">✦</span></p></div></div>
      </div>
      <nav className="mt-8 flex-1 space-y-1">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} onClick={() => setOpen(false)} className={({ isActive }) => cx('flex items-center gap-3 rounded-2xl px-4 py-3 text-sm font-semibold transition', isActive ? 'bg-primary text-white shadow-lg shadow-primary/15' : 'text-slate-500 hover:bg-sky hover:text-ink')}><Icon size={18} />{label}</NavLink>)}</nav>
      <div className="rounded-2xl border border-slate-100 bg-[#fffaf0] p-4"><div className="flex items-start gap-2 text-sun-700"><CircleHelp size={18} className="mt-0.5 text-amber-500" /><p className="text-xs leading-5 text-slate-600">If you are in immediate danger, please contact local emergency services.</p></div></div>
      <button onClick={() => { localStorage.clear(); navigate('/'); }} className="mt-5 flex items-center gap-3 px-4 py-2 text-sm font-semibold text-slate-400 hover:text-ink"><LogOut size={17} /> Sign out</button>
    </aside>
    {open && <button aria-label="Close menu" className="fixed inset-0 z-20 bg-ink/20 lg:hidden" onClick={() => setOpen(false)} />}
    <main className="lg:pl-72">
      <header className="sticky top-0 z-10 flex h-20 items-center justify-between border-b border-slate-100/80 bg-[#f7fbff]/90 px-5 backdrop-blur lg:px-10">
        <button className="rounded-full border border-slate-200 bg-white p-2 text-ink lg:hidden" onClick={() => setOpen(true)}><Menu size={20} /></button>
        <div className="hidden text-sm text-slate-400 md:block">{location.pathname === '/dashboard' ? 'Your gentle space to check in' : 'A little room to breathe'}</div>
        <div className="ml-auto flex items-center gap-3"><Link to="/coins" className="flex items-center gap-2 rounded-full border border-amber-200 bg-[#fffaf0] px-3 py-2 text-sm font-bold text-amber-700"><Coins size={16} />{user?.calm_coins || 240}</Link><div className="grid h-9 w-9 place-items-center rounded-full bg-primary/10 text-sm font-bold text-primary">{(user?.full_name || 'A')[0]}</div></div>
      </header>
      <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10">{children}</div>
    </main>
  </div>
}

function Landing() {
  const navigate = useNavigate()
  return <div className="overflow-hidden bg-[#f7fbff]">
    <header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-10"><Logo /><nav className="hidden items-center gap-8 text-sm font-semibold text-slate-500 md:flex"><a href="#how-it-works">How it works</a><a href="#features">Features</a><a href="#promise">Our promise</a></nav><div className="flex items-center gap-3"><Link to="/login" className="hidden px-3 py-2 text-sm font-bold text-ink sm:block">Sign in</Link><Button onClick={() => navigate('/register')}>Start for free <ArrowRight size={16} /></Button></div></header>
    <section className="relative mx-auto grid max-w-7xl items-center gap-10 px-5 pb-20 pt-12 lg:grid-cols-[1.05fr_.95fr] lg:px-10 lg:pb-32 lg:pt-20">
      <div className="relative z-10"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-primary/10 bg-white px-3 py-2 text-xs font-bold text-primary shadow-sm"><span className="flex h-5 w-5 items-center justify-center rounded-full bg-mint text-teal-700">✦</span> Made for your real life</div><h1 className="max-w-2xl text-5xl font-extrabold leading-[1.05] tracking-[-.04em] text-ink sm:text-6xl lg:text-7xl">A softer way to take care of <span className="relative text-primary">yourself<span className="absolute -bottom-1 left-1/2 h-2 w-28 -translate-x-1/2 rounded-full bg-[#bce9e2]"></span></span>.</h1><p className="mt-7 max-w-xl text-lg leading-8 text-slate-500">ZenHeaven brings supportive conversations, mindful tools, and small moments of calm into one friendly space.</p><div className="mt-9 flex flex-wrap gap-3"><Button onClick={() => navigate('/register')}>Find your calm <ArrowRight size={17} /></Button><button onClick={() => document.querySelector('#how-it-works')?.scrollIntoView({ behavior: 'smooth' })} className="inline-flex items-center gap-2 rounded-full px-5 py-3 text-sm font-bold text-ink hover:bg-white"><Play size={16} className="fill-primary text-primary" /> See how it works</button></div><div className="mt-9 flex items-center gap-3"><div className="flex -space-x-2">{['#f4b3a8', '#8fbee9', '#f7d889', '#9fdace'].map((color, i) => <span key={color} className="grid h-8 w-8 place-items-center rounded-full border-2 border-[#f7fbff] text-xs" style={{ backgroundColor: color }}>{['A', 'J', 'M', 'S'][i]}</span>)}</div><p className="text-xs text-slate-500"><span className="font-bold text-ink">12,000+</span> people making space for themselves</p></div></div>
      <div className="relative min-h-[450px]"><div className="absolute right-0 top-0 h-[390px] w-[390px] rounded-[45%] bg-[#dff3ff] blur-[1px] lg:h-[530px] lg:w-[530px]"></div><div className="absolute bottom-8 right-10 h-72 w-72 rounded-full bg-[#fff0c9] blur-3xl"></div><div className="relative mx-auto mt-10 max-w-md lg:mt-0"><div className="relative overflow-hidden rounded-[42%_42%_25%_25%] border-[10px] border-white bg-[#bfe7f0] shadow-soft"><svg viewBox="0 0 500 520" className="block w-full"><path d="M0 390c100-50 185-32 256-4 80 31 155 18 244-31v165H0z" fill="#9edbd2" /><circle cx="350" cy="90" r="52" fill="#f8c2ad" /><path d="M302 91c5-53 91-72 112-9-18-16-36-12-52 2-10 9-21 14-60 7z" fill="#294e65" /><circle cx="334" cy="98" r="4" fill="#123b60" /><circle cx="372" cy="98" r="4" fill="#123b60" /><path d="M343 116q12 10 24 0" stroke="#123b60" strokeWidth="4" fill="none" strokeLinecap="round" /><path d="M285 160q63-31 127 0v125H285z" fill="#2778d4" /><path d="M298 157q50 40 100 0" fill="#f8c2ad" /><path d="M302 284q-16 68-4 148h42l24-98 25 98h42q9-80-10-148z" fill="#e87f65" /><path d="M291 178q-47 16-64 96l25 12 38-66M410 176q43 24 45 94l-29 4-24-64" fill="#f8c2ad" /><path d="M222 279l35 6-10 12-31-3zM422 273l33-1 5 12-34 4z" fill="#f8c2ad" /></svg><div className="absolute bottom-5 left-5 rounded-2xl bg-white/90 px-4 py-3 shadow-lg backdrop-blur"><div className="flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-full bg-mint text-teal-700"><Heart size={14} fill="currentColor" /></span><div><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Today's intention</p><p className="text-xs font-bold text-ink">Be gentle with myself</p></div></div></div></div><div className="absolute -right-5 top-20 rounded-2xl bg-white px-4 py-3 shadow-soft"><div className="flex items-center gap-2 text-xs font-bold text-ink"><span className="grid h-7 w-7 place-items-center rounded-xl bg-[#fff0c9] text-amber-600"><Sun size={15} /></span> You showed up today</div></div><div className="absolute -left-5 bottom-16 rounded-2xl bg-white px-4 py-3 shadow-soft"><div className="flex items-center gap-2 text-xs font-bold text-ink"><span className="grid h-7 w-7 place-items-center rounded-xl bg-sky text-primary"><Sparkles size={15} /></span> +10 calm coins</div></div></div></div>
    </section>
    <section className="border-y border-slate-100 bg-white"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-center gap-x-12 gap-y-4 px-5 py-6 text-xs font-bold uppercase tracking-[.15em] text-slate-400 lg:px-10"><span className="flex items-center gap-2"><ShieldCheck size={17} className="text-primary" /> Private by design</span><span className="flex items-center gap-2"><Heart size={17} className="text-rose-400" /> Human-first support</span><span className="flex items-center gap-2"><LockKeyhole size={17} className="text-teal-500" /> Your space, your pace</span></div></section>
    <section id="features" className="mx-auto max-w-7xl px-5 py-24 lg:px-10"><div className="max-w-xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Everything you need</p><h2 className="mt-3 text-4xl font-extrabold tracking-tight text-ink sm:text-5xl">Small steps. Real support.</h2><p className="mt-4 leading-7 text-slate-500">There is no perfect way to feel better. ZenHeaven gives you a few good places to begin.</p></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4"><FeatureCard icon={MessageCircle} color="bg-sky text-primary" title="Talk it out" text="A caring AI companion for the moments you need to be heard." to="/chat" /><FeatureCard icon={NotebookPen} color="bg-mint text-teal-700" title="Make room" text="Journal freely, notice patterns, and celebrate tiny wins." to="/journal" /><FeatureCard icon={Headphones} color="bg-[#fff0c9] text-amber-700" title="Find your sound" text="Mood-friendly music to meet you exactly where you are." to="/music" /><FeatureCard icon={Users} color="bg-coral text-rose-500" title="Feel supported" text="Connect with thoughtful, licensed therapists when you’re ready." to="/therapists" /></div></section>
    <section id="how-it-works" className="bg-ink py-24 text-white"><div className="mx-auto max-w-7xl px-5 lg:px-10"><div className="grid items-end gap-10 lg:grid-cols-[.9fr_1.1fr]"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-sky-200">How it works</p><h2 className="mt-3 text-4xl font-extrabold tracking-tight sm:text-5xl">Start where you are.</h2><p className="mt-5 max-w-md leading-7 text-blue-100/70">Your wellness journey is not a straight line. Pick a door, take your time, and come back whenever you need.</p><Button variant="light" className="mt-8" onClick={() => navigate('/register')}>Create your free space <ArrowRight size={16} /></Button></div><div className="grid gap-4 sm:grid-cols-3">{[['01', 'Check in', 'Name what is here, without judgment.'], ['02', 'Choose a tool', 'A conversation, a page, a song, or support.'], ['03', 'Keep going', 'Collect small moments that add up.']].map(([num, title, text]) => <div key={num} className="rounded-3xl border border-white/10 bg-white/5 p-6"><span className="text-sm font-bold text-sky-200">{num}</span><h3 className="mt-16 text-xl font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-blue-100/65">{text}</p></div>)}</div></div></div></section>
    <section id="promise" className="mx-auto grid max-w-7xl gap-10 px-5 py-24 lg:grid-cols-2 lg:items-center lg:px-10"><div className="rounded-[2.5rem] bg-[#e6f8f4] p-8 sm:p-12"><div className="mb-12 inline-flex rounded-full bg-white px-4 py-2 text-xs font-bold text-teal-700"><Sparkles size={14} className="mr-2" /> A little promise from us</div><blockquote className="text-3xl font-extrabold leading-tight tracking-tight text-ink sm:text-4xl">“You do not have to be at your best to deserve support.”</blockquote><p className="mt-8 text-sm font-semibold text-teal-800">— The ZenHeaven team</p></div><div><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Built with care</p><h2 className="mt-3 text-4xl font-extrabold tracking-tight text-ink">A safe place for your in-between moments.</h2><p className="mt-5 leading-8 text-slate-500">ZenHeaven is an everyday wellness companion, not a replacement for professional care. When you need more, we make it easier to find qualified human support.</p><div className="mt-8 space-y-4">{['Thoughtful tools, never pressure', 'Clear boundaries and crisis resources', 'Progress that feels personal, not performative'].map((item) => <div key={item} className="flex items-center gap-3 text-sm font-bold text-ink"><span className="grid h-6 w-6 place-items-center rounded-full bg-mint text-teal-700"><Check size={14} /></span>{item}</div>)}</div></div></section>
    <footer className="border-t border-slate-100 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-5 px-5 py-8 text-sm text-slate-400 sm:flex-row sm:items-center sm:justify-between lg:px-10"><Logo /><span>© 2025 ZenHeaven. Made for gentler days.</span><span className="flex items-center gap-2">You are not alone <Heart size={14} className="text-rose-400" fill="currentColor" /></span></div></footer>
  </div>
}

function FeatureCard({ icon: Icon, color, title, text, to }) {
  return <Link to={to} className="group rounded-3xl border border-slate-100 bg-white p-6 shadow-card transition hover:-translate-y-1 hover:shadow-soft"><span className={cx('grid h-12 w-12 place-items-center rounded-2xl', color)}><Icon size={22} /></span><h3 className="mt-8 text-lg font-extrabold text-ink">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p><span className="mt-6 inline-flex items-center gap-2 text-xs font-bold text-primary">Explore <ArrowRight size={14} className="transition group-hover:translate-x-1" /></span></Link>
}

function AuthPage({ mode = 'login' }) {
  const navigate = useNavigate()
  const [form, setForm] = useState({ username: '', email: '', password: '', full_name: '' })
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const isRegister = mode === 'register'
  const submit = async (event) => {
    event.preventDefault(); setLoading(true); setError('')
    try {
      const result = isRegister ? await auth.register(form) : await auth.login(form.username, form.password)
      localStorage.setItem('zenheaven_token', result.access_token); localStorage.setItem('zenheaven_user', JSON.stringify(result.user)); navigate('/dashboard')
    } catch (err) { setError(err.message) } finally { setLoading(false) }
  }
  return <div className="grid min-h-screen bg-[#f7fbff] lg:grid-cols-2"><div className="hidden overflow-hidden bg-ink p-12 lg:flex lg:flex-col lg:justify-between"><Logo light /><div className="relative mx-auto w-full max-w-lg"><div className="absolute -left-10 top-10 h-56 w-56 rounded-full bg-primary/30 blur-3xl"></div><div className="relative rounded-[3rem] border border-white/10 bg-white/5 p-10"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-[#fff0c9] text-amber-700"><Sun size={26} /></span><p className="mt-10 text-4xl font-extrabold leading-tight">A kinder day can start with one small check-in.</p><p className="mt-5 leading-7 text-blue-100/65">Your private space for conversations, reflections, and finding your next gentle step.</p><div className="mt-12 flex items-center gap-3 text-sm font-bold text-sky-100"><span className="grid h-8 w-8 place-items-center rounded-full bg-mint text-teal-700"><Check size={15} /></span>No pressure. No perfection.</div></div></div><p className="text-sm text-blue-100/50">Take what helps. Leave what doesn’t.</p></div><div className="flex flex-col px-5 py-6 sm:px-12 lg:px-20"><Link to="/" className="lg:hidden"><Logo /></Link><div className="mx-auto flex w-full max-w-md flex-1 flex-col justify-center py-10"><Link to="/" className="mb-10 hidden lg:block"><Logo /></Link><p className="text-sm font-bold text-primary">{isRegister ? 'Welcome, you' : 'Welcome back'}</p><h1 className="mt-2 text-4xl font-extrabold tracking-tight text-ink">{isRegister ? 'Make space for yourself.' : 'Good to see you again.'}</h1><p className="mt-3 text-slate-500">{isRegister ? 'Your gentle corner of the internet is waiting.' : 'Let’s take a breath and see how you’re doing.'}</p><form onSubmit={submit} className="mt-9 space-y-4">{isRegister && <Field label="What should we call you?" placeholder="Your name" value={form.full_name} onChange={(value) => setForm({ ...form, full_name: value })} />}<Field label="Username" placeholder="your-username" value={form.username} onChange={(value) => setForm({ ...form, username: value })} required /><>{isRegister && <Field label="Email address" type="email" placeholder="you@example.com" value={form.email} onChange={(value) => setForm({ ...form, email: value })} required />}</><Field label="Password" type="password" placeholder="At least 6 characters" value={form.password} onChange={(value) => setForm({ ...form, password: value })} required />{error && <p className="rounded-xl bg-coral px-4 py-3 text-sm font-semibold text-rose-600">{error}</p>}<Button className="mt-2 w-full" disabled={loading}>{loading ? <LoaderCircle size={17} className="animate-spin" /> : isRegister ? 'Create my space' : 'Sign in'} <ArrowRight size={16} /></Button></form><p className="mt-7 text-center text-sm text-slate-500">{isRegister ? 'Already have a space?' : 'New to ZenHeaven?'} <Link className="font-bold text-primary" to={isRegister ? '/login' : '/register'}>{isRegister ? 'Sign in' : 'Create one'}</Link></p><p className="mt-12 flex items-center justify-center gap-2 text-center text-xs text-slate-400"><LockKeyhole size={13} /> Your wellbeing data belongs to you.</p></div></div></div>
}

function Field({ label, type = 'text', placeholder, value, onChange, required }) {
  return <label className="block text-sm font-bold text-ink">{label}<input required={required} type={type} placeholder={placeholder} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3.5 font-medium text-ink outline-none transition placeholder:text-slate-300 focus:border-primary focus:ring-4 focus:ring-primary/10" /></label>
}

function PageHeader({ eyebrow, title, text, action }) {
  return <div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-primary">{eyebrow}</p><h1 className="mt-2 text-4xl font-extrabold tracking-tight text-ink">{title}</h1>{text && <p className="mt-3 max-w-2xl leading-7 text-slate-500">{text}</p>}</div>{action}</div>
}

function Dashboard() {
  const user = getStoredUser()
  const [balance, setBalance] = useState(user?.calm_coins || 240)
  useEffect(() => { coins.balance().then((result) => setBalance(result.balance)).catch(() => {}) }, [])
  const actions = [{ to: '/chat', icon: MessageCircle, title: 'Talk to CalmBot', text: 'A listening ear, anytime.', color: 'bg-sky text-primary' }, { to: '/journal', icon: NotebookPen, title: 'Write it down', text: 'Make room for what’s here.', color: 'bg-mint text-teal-700' }, { to: '/music', icon: Music2, title: 'Set the mood', text: 'Find a softer soundtrack.', color: 'bg-[#fff0c9] text-amber-700' }]
  return <><PageHeader eyebrow="Tuesday, a fresh page" title={`How are you, ${user?.full_name?.split(' ')[0] || 'friend'}?`} text="There is no right way to use this space. Follow what feels useful today." action={<Link to="/journal" className="hidden text-sm font-bold text-primary sm:flex sm:items-center sm:gap-2">View your journal <ArrowRight size={15} /></Link>} /><div className="mt-10 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><Card className="overflow-hidden bg-ink p-7 text-white sm:p-9"><div className="flex items-start justify-between"><div><p className="text-sm font-bold text-sky-200">Your daily check-in</p><h2 className="mt-4 max-w-md text-3xl font-extrabold leading-tight">What would feel supportive right now?</h2></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white/10 text-[#fff0c9]"><Sparkles size={23} /></div></div><div className="mt-8 flex flex-wrap gap-2">{['A calm moment', 'A listening ear', 'A little motivation'].map((label) => <Link key={label} to={label === 'A listening ear' ? '/chat' : '/journal'} className="rounded-full border border-white/15 bg-white/10 px-4 py-2 text-sm font-semibold text-blue-50 transition hover:bg-white/20">{label}</Link>)}</div><div className="mt-10 flex items-center gap-2 text-xs font-semibold text-blue-100/60"><span className="h-2 w-2 rounded-full bg-[#8de0c6]"></span> Your space is private and pressure-free</div></Card><Card className="bg-[#fffaf0] p-7"><div className="flex items-center justify-between"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#ffe6a4] text-amber-700"><Coins size={22} /></span><Link to="/coins" className="text-xs font-bold text-amber-700">See activity <ArrowRight size={13} className="inline" /></Link></div><p className="mt-7 text-sm font-semibold text-slate-500">Calm coins</p><p className="mt-1 text-4xl font-extrabold text-ink">{balance}</p><div className="mt-6 h-2 overflow-hidden rounded-full bg-amber-100"><div className="h-full w-[68%] rounded-full bg-amber-400"></div></div><p className="mt-3 text-xs font-semibold text-slate-500">Keep showing up — small steps count.</p></Card></div><div className="mt-10"><div className="flex items-center justify-between"><h2 className="text-xl font-extrabold text-ink">Choose your next small step</h2><span className="text-xs font-bold text-slate-400">3 gentle options</span></div><div className="mt-4 grid gap-4 md:grid-cols-3">{actions.map(({ to, icon: Icon, title, text, color }) => <Link to={to} key={to} className="group rounded-3xl border border-slate-100 bg-white p-5 shadow-card transition hover:-translate-y-1 hover:shadow-soft"><span className={cx('grid h-11 w-11 place-items-center rounded-2xl', color)}><Icon size={21} /></span><h3 className="mt-7 font-extrabold text-ink">{title}</h3><p className="mt-1 text-sm text-slate-500">{text}</p><ArrowRight size={16} className="mt-5 text-primary transition group-hover:translate-x-1" /></Link>)}</div></div><div className="mt-10 grid gap-5 md:grid-cols-2"><Card className="p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-mint text-teal-700"><BookHeart size={19} /></span><div><h3 className="font-extrabold text-ink">A thought to carry</h3><p className="text-xs text-slate-400">For the rest of today</p></div></div><Sparkles size={17} className="text-amber-500" /></div><p className="mt-6 text-lg font-bold leading-7 text-ink">“You can go slowly and still be moving forward.”</p></Card><Card className="flex items-center justify-between gap-5 bg-sky p-6"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">Need more support?</p><h3 className="mt-2 text-xl font-extrabold text-ink">Find a therapist who gets it.</h3><Link to="/therapists" className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-primary">Browse therapists <ArrowRight size={15} /></Link></div><div className="hidden h-20 w-20 shrink-0 place-items-center rounded-full bg-white text-primary sm:grid"><Users size={30} /></div></Card></div></>
}

function ChatPage() {
  const [threads, setThreads] = useState([])
  const [activeId, setActiveId] = useState(null)
  const [messages, setMessages] = useState([{ id: 'welcome', content: 'Hi, I’m CalmBot. You can bring me the messy version — what feels present today?', is_user: false }])
  const [input, setInput] = useState('')
  const [thinking, setThinking] = useState('')
  const [sending, setSending] = useState(false)
  useEffect(() => { chat.threads().then((data) => setThreads(data.threads || [])).catch(() => {}) }, [])
  const selectThread = async (id) => { setActiveId(id); const data = await chat.thread(id); setMessages(data.messages || []) }
  const send = async (event) => {
    event.preventDefault(); const message = input.trim(); if (!message || sending) return
    setInput(''); setSending(true); setMessages((prev) => [...prev, { id: `user-${Date.now()}`, content: message, is_user: true }])
    let response = ''; let threadId = activeId
    await chat.send(message, activeId, (eventData) => {
      if (eventData.type === 'thinking') setThinking(eventData.data)
      if (eventData.type === 'thread_id') { threadId = eventData.data; setActiveId(eventData.data) }
      if (eventData.type === 'response_start') { setThinking(''); setMessages((prev) => [...prev, { id: `bot-${Date.now()}`, content: '', is_user: false }]) }
      if (eventData.type === 'token') { response += eventData.data; setMessages((prev) => prev.map((item) => item.id.startsWith('bot-') ? { ...item, content: response } : item)) }
      if (eventData.type === 'complete') {
        if (eventData.data?.fallback_response) { response = eventData.data.fallback_response; setMessages((prev) => [...prev, { id: `bot-${Date.now()}`, content: response, is_user: false }]) }
        setThinking('')
      }
    })
    if (!response) setMessages((prev) => [...prev, { id: `bot-${Date.now()}`, content: 'I’m here with you. Would you like to tell me a little more?', is_user: false }])
    setSending(false); if (threadId && !threads.some((thread) => thread.id === threadId)) setThreads((prev) => [{ id: threadId, title: 'A new check-in', last_message: message }, ...prev])
  }
  return <><PageHeader eyebrow="A listening space" title="Talk it out" text="Share what’s on your mind. CalmBot is here to listen and help you find a next step." /><div className="mt-8 grid min-h-[620px] gap-5 lg:grid-cols-[260px_1fr]"><Card className="hidden p-4 lg:block"><div className="flex items-center justify-between px-2"><h3 className="font-extrabold text-ink">Your chats</h3><button onClick={() => { setActiveId(null); setMessages([{ id: 'welcome', content: 'Hi, I’m CalmBot. What feels present today?', is_user: false }]) }} className="rounded-full bg-sky p-2 text-primary"><Plus size={16} /></button></div><div className="mt-5 space-y-1">{threads.map((thread) => <button key={thread.id} onClick={() => selectThread(thread.id)} className={cx('w-full rounded-2xl px-3 py-3 text-left transition', activeId === thread.id ? 'bg-sky' : 'hover:bg-slate-50')}><p className="truncate text-sm font-bold text-ink">{thread.title}</p><p className="mt-1 truncate text-xs text-slate-400">{thread.last_message || 'No messages yet'}</p></button>)}{threads.length === 0 && <p className="px-3 py-4 text-xs leading-5 text-slate-400">Your conversations will show up here.</p>}</div></Card><Card className="flex min-h-[620px] flex-col overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4 sm:px-7"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-mint text-teal-700"><Sparkles size={19} /></span><div><h2 className="font-extrabold text-ink">CalmBot</h2><p className="flex items-center gap-1 text-xs font-semibold text-teal-600"><span className="h-1.5 w-1.5 rounded-full bg-teal-400"></span> Here with you</p></div></div><span className="hidden rounded-full bg-sky px-3 py-1.5 text-[11px] font-bold text-primary sm:block">5 coins per check-in</span></div><div className="flex-1 space-y-5 overflow-y-auto px-5 py-7 sm:px-10">{messages.map((message) => <div key={message.id} className={cx('flex max-w-[85%] gap-3', message.is_user ? 'ml-auto flex-row-reverse' : '')}><span className={cx('grid h-8 w-8 shrink-0 place-items-center rounded-full text-xs font-bold', message.is_user ? 'bg-primary text-white' : 'bg-mint text-teal-700')}>{message.is_user ? 'A' : <Sparkles size={14} />}</span><div className={cx('rounded-2xl px-4 py-3 text-sm leading-6', message.is_user ? 'rounded-tr-sm bg-primary text-white' : 'rounded-tl-sm bg-slate-50 text-ink')}>{message.content}</div></div>)}{thinking && <div className="flex items-center gap-2 text-xs font-semibold text-slate-400"><span className="flex gap-1"><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-primary"></i><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-primary [animation-delay:120ms]"></i><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-primary [animation-delay:240ms]"></i></span>{thinking}</div>}</div><form onSubmit={send} className="border-t border-slate-100 bg-white p-4 sm:p-6"><div className="flex items-end gap-2 rounded-2xl border border-slate-200 bg-[#f7fbff] p-2 focus-within:border-primary"><textarea value={input} onChange={(event) => setInput(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); send(event) } }} rows="1" placeholder="Tell me what’s on your mind…" className="max-h-28 flex-1 resize-none bg-transparent px-3 py-2 text-sm text-ink outline-none placeholder:text-slate-400" /><button disabled={!input.trim() || sending} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-primary text-white transition hover:bg-primary-dark disabled:opacity-40"><Send size={17} /></button></div><p className="mt-3 text-center text-[10px] text-slate-400">CalmBot offers support and reflection, not medical advice or emergency care.</p></form></Card></div></>
}

function JournalPage() {
  const [entries, setEntries] = useState([])
  const [prompts, setPrompts] = useState([])
  const [content, setContent] = useState('')
  const [mood, setMood] = useState('calm')
  const [saving, setSaving] = useState(false)
  useEffect(() => { journal.entries().then(setEntries); journal.prompts().then(setPrompts) }, [])
  const save = async (event) => { event.preventDefault(); if (!content.trim()) return; setSaving(true); const entry = await journal.create(content, mood); setEntries((prev) => [entry, ...prev]); setContent(''); setSaving(false) }
  return <><PageHeader eyebrow="Your private pages" title="Journal" text="Put the day down somewhere safe. It doesn’t have to be polished to be helpful." action={<span className="flex items-center gap-2 rounded-full bg-mint px-4 py-2 text-xs font-bold text-teal-700"><LockKeyhole size={14} /> Just for you</span>} /><div className="mt-9 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><Card className="p-6 sm:p-8"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-mint text-teal-700"><NotebookPen size={21} /></span><div><h2 className="font-extrabold text-ink">How are things feeling?</h2><p className="text-xs text-slate-400">A few honest words are enough.</p></div></div><form onSubmit={save} className="mt-7"><textarea value={content} onChange={(event) => setContent(event.target.value)} rows="8" placeholder="Start wherever feels easiest…" className="w-full resize-none rounded-2xl border border-slate-200 bg-[#fbfdff] p-4 text-sm leading-7 text-ink outline-none placeholder:text-slate-300 focus:border-primary focus:ring-4 focus:ring-primary/10"></textarea><div className="mt-5 flex flex-wrap items-center gap-2"><span className="mr-1 text-xs font-bold text-slate-400">Today feels</span>{['calm', 'hopeful', 'tired', 'anxious'].map((item) => <button type="button" key={item} onClick={() => setMood(item)} className={cx('rounded-full px-3 py-1.5 text-xs font-bold capitalize transition', mood === item ? 'bg-primary text-white' : 'bg-slate-100 text-slate-500 hover:bg-sky')}>{item}</button>)}<Button className="ml-auto" disabled={saving || !content.trim()}>{saving ? <LoaderCircle size={16} className="animate-spin" /> : <><span>Save entry</span><ArrowRight size={15} /></>}</Button></div></form></Card><Card className="bg-[#fffaf0] p-6 sm:p-8"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#ffe6a4] text-amber-700"><Lightbulb size={21} /></span><div><h2 className="font-extrabold text-ink">A gentle prompt</h2><p className="text-xs text-slate-400">No pressure to answer.</p></div></div><p className="mt-9 text-2xl font-extrabold leading-tight text-ink">“{prompts[0]?.prompt || 'What would feel kind to yourself today?'}”</p><button onClick={() => setPrompts((prev) => [...prev.slice(1), ...prev.slice(0, 1)])} className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-amber-700">Give me another <ArrowRight size={15} /></button></Card></div><div className="mt-10 flex items-center justify-between"><h2 className="text-xl font-extrabold text-ink">Recent reflections</h2><span className="text-xs font-bold text-slate-400">{entries.length} entries</span></div><div className="mt-4 grid gap-4 md:grid-cols-2">{entries.map((entry) => <Card key={entry._id || entry.id} className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">{entry.mood || 'reflective'}</p><h3 className="mt-2 text-lg font-extrabold text-ink">{entry.title || 'A moment worth keeping'}</h3></div><span className="rounded-full bg-slate-50 px-3 py-1 text-[11px] font-semibold text-slate-400">{entry.created_at ? new Date(entry.created_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric' }) : 'Today'}</span></div><p className="mt-5 line-clamp-3 text-sm leading-6 text-slate-500">{entry.content}</p></Card>)}</div></>
}

function BooksPage() {
  const [data, setData] = useState({ books: [], mood: 'calm', mood_description: '' }); const [query, setQuery] = useState(''); const [loading, setLoading] = useState(false)
  useEffect(() => { books.recommendations().then(setData) }, [])
  const search = async (event) => { event.preventDefault(); if (!query.trim()) return; setLoading(true); const result = await books.search(query); setData((prev) => ({ ...prev, books: result.books || [] })); setLoading(false) }
  return <><PageHeader eyebrow="A little perspective" title="Books for your mood" text="Stories and ideas to meet you where you are — whether you’re looking for comfort, clarity, or a spark." /><div className="mt-8 flex flex-col gap-4 rounded-3xl bg-sky p-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">Picked for feeling {data.mood}</p><p className="mt-2 font-bold text-ink">{data.mood_description || 'A few thoughtful pages for your next quiet moment.'}</p></div><form onSubmit={search} className="flex rounded-full border border-white bg-white p-1"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search books" className="w-36 bg-transparent px-3 text-sm outline-none placeholder:text-slate-400 sm:w-44" /><button className="grid h-9 w-9 place-items-center rounded-full bg-primary text-white"><Search size={15} /></button></form></div><div className="mt-9 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{data.books.map((book) => <Card key={book.id} className="group overflow-hidden"><div className="relative h-56 overflow-hidden bg-sky">{book.image_url ? <img src={book.image_url} alt="" className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /> : <div className="grid h-full place-items-center text-primary"><BookOpen size={44} /></div>}<span className="absolute left-3 top-3 rounded-full bg-white/90 px-2.5 py-1 text-[10px] font-bold uppercase text-primary">For you</span></div><div className="p-5"><h3 className="line-clamp-2 font-extrabold leading-5 text-ink">{book.title}</h3><p className="mt-2 text-xs font-bold text-primary">{book.author}</p><p className="mt-3 line-clamp-2 text-xs leading-5 text-slate-500">{book.description}</p></div></Card>)}{loading && <div className="col-span-full flex justify-center py-10"><LoaderCircle className="animate-spin text-primary" /></div>}</div></>
}

function MusicPage() {
  const [songs, setSongs] = useState([]); const [selected, setSelected] = useState(''); const [recommendations, setRecommendations] = useState([]); const [playing, setPlaying] = useState(null)
  useEffect(() => { music.songs().then((data) => { setSongs(data.songs || []); setSelected(data.songs?.[0] || '') }) }, [])
  const recommend = async () => { if (selected) { const data = await music.recommend(selected); setRecommendations(data.recommendations || []) } }
  return <><PageHeader eyebrow="Soundtrack your day" title="Music that meets you there" text="Choose a song you already love and we’ll find a few gentle companions for the feeling." /><Card className="mt-8 overflow-hidden bg-ink p-7 text-white sm:p-10"><div className="grid items-center gap-8 lg:grid-cols-[1fr_300px]"><div><div className="flex items-center gap-2 text-sm font-bold text-sky-200"><Music2 size={18} /> Mood-based recommendations</div><h2 className="mt-5 max-w-lg text-3xl font-extrabold leading-tight">What sounds like you today?</h2><div className="mt-7 flex max-w-md gap-2 rounded-2xl bg-white p-1.5"><select value={selected} onChange={(event) => setSelected(event.target.value)} className="min-w-0 flex-1 bg-transparent px-3 text-sm font-semibold text-ink outline-none">{songs.map((song) => <option key={song}>{song}</option>)}</select><Button onClick={recommend}>Find my mix <ArrowRight size={15} /></Button></div></div><div className="relative hidden h-44 lg:block"><div className="absolute right-8 top-0 h-32 w-32 rounded-full bg-[#ffdc8c]/80"></div><div className="absolute bottom-0 left-12 h-24 w-24 rounded-full bg-[#70d0c0]/50"></div><div className="absolute left-0 top-10 rotate-[-12deg] text-[110px] leading-none">♫</div></div></div></Card><div className="mt-10 flex items-center justify-between"><h2 className="text-xl font-extrabold text-ink">{recommendations.length ? 'A few songs for this feeling' : 'A small starting shelf'}</h2><span className="text-xs font-bold text-slate-400">Press play when ready</span></div><div className="mt-4 grid gap-4 md:grid-cols-2">{(recommendations.length ? recommendations : [{ name: 'Weightless', artist: 'Marconi Union' }, { name: 'Bloom', artist: 'The Paper Kites' }, { name: 'Anchor', artist: 'Novo Amor' }, { name: 'Holocene', artist: 'Bon Iver' }]).map((song, index) => <div key={`${song.name}-${index}`} className="flex items-center gap-4 rounded-3xl border border-slate-100 bg-white p-4 shadow-card"><div className={cx('grid h-14 w-14 shrink-0 place-items-center overflow-hidden rounded-2xl', index % 2 ? 'bg-mint text-teal-700' : 'bg-sky text-primary')}>{song.album_cover_url ? <img src={song.album_cover_url} alt="" className="h-full w-full object-cover" /> : <Music2 size={23} />}</div><div className="min-w-0 flex-1"><p className="truncate font-extrabold text-ink">{song.name}</p><p className="mt-1 text-xs font-semibold text-slate-400">{song.artist || 'A soft place to land'}</p></div><button onClick={() => setPlaying(playing === song.name ? null : song.name)} className={cx('grid h-10 w-10 place-items-center rounded-full transition', playing === song.name ? 'bg-primary text-white' : 'bg-sky text-primary hover:bg-primary hover:text-white')}>{playing === song.name ? <span className="flex gap-0.5"><i className="h-3 w-0.5 animate-pulse bg-current"></i><i className="h-4 w-0.5 animate-pulse bg-current [animation-delay:100ms]"></i><i className="h-2 w-0.5 animate-pulse bg-current [animation-delay:200ms]"></i></span> : <Play size={15} fill="currentColor" />}</button></div>)}</div></>
}

function TherapistsPage() {
  const [items, setItems] = useState([]); const [filter, setFilter] = useState('All')
  useEffect(() => { therapists.list().then(setItems) }, [])
  const specializations = useMemo(() => ['All', ...new Set(items.flatMap((item) => item.specializations || []))], [items])
  const filtered = filter === 'All' ? items : items.filter((item) => item.specializations?.includes(filter))
  return <><PageHeader eyebrow="Human support" title="Find your person" text="When you’re ready for one-on-one support, browse thoughtful therapists and find a style that feels like a fit." /><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{specializations.slice(0, 7).map((item) => <button key={item} onClick={() => setFilter(item)} className={cx('whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold transition', filter === item ? 'bg-primary text-white' : 'bg-white text-slate-500 shadow-sm hover:bg-sky')}>{item}</button>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-3">{filtered.map((therapist) => <Card key={therapist._id || therapist.id} className="overflow-hidden"><div className="flex gap-4 bg-sky p-5"><img src={therapist.photo_url} alt="" className="h-20 w-20 rounded-2xl object-cover" /><div><div className="flex items-center gap-1 text-xs font-bold text-amber-600"><Star size={13} fill="currentColor" /> {therapist.rating}</div><h2 className="mt-1 font-extrabold text-ink">{therapist.name}</h2><p className="mt-1 text-xs font-semibold text-slate-500">{therapist.experience_years} years experience</p></div></div><div className="p-5"><div className="flex flex-wrap gap-1.5">{therapist.specializations?.map((tag) => <span key={tag} className="rounded-full bg-mint px-2.5 py-1 text-[10px] font-bold text-teal-700">{tag}</span>)}</div><p className="mt-5 text-sm leading-6 text-slate-500">{therapist.bio}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="text-xs font-semibold text-slate-400"><span className="font-extrabold text-ink">${therapist.hourly_rate}</span> / session</span><Button className="px-4 py-2.5 text-xs" onClick={() => alert(`We’ll help you book with ${therapist.name}.`)}>View profile <ArrowRight size={14} /></Button></div></div></Card>)}</div><div className="mt-8 flex items-start gap-3 rounded-2xl border border-[#f6dfad] bg-[#fffaf0] p-5 text-sm text-slate-600"><CircleHelp size={18} className="mt-0.5 shrink-0 text-amber-500" /><p><span className="font-bold text-ink">A gentle reminder:</span> ZenHeaven is not for emergencies. If you may hurt yourself or someone else, please call your local emergency number now.</p></div></>
}

function CoinsPage() {
  const [balance, setBalance] = useState(240); const [transactions, setTransactions] = useState([])
  useEffect(() => { coins.balance().then((data) => setBalance(data.balance)); coins.transactions().then(setTransactions) }, [])
  return <><PageHeader eyebrow="Celebrate showing up" title="Calm coins" text="A tiny thank-you for the care you put toward yourself. Earn them through small, supportive actions." /><div className="mt-8 grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><Card className="relative overflow-hidden bg-ink p-8 text-white"><div className="absolute -right-10 -top-10 h-44 w-44 rounded-full bg-primary/30"></div><div className="relative"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-[#ffe6a4] text-amber-700"><Coins size={27} /></span><p className="mt-10 text-sm font-semibold text-blue-100/70">Your current balance</p><p className="mt-1 text-6xl font-extrabold tracking-tight">{balance}</p><p className="mt-6 text-sm text-blue-100/60">Keep going at your own pace.</p></div></Card><Card className="p-7"><div className="flex items-center justify-between"><div><h2 className="text-xl font-extrabold text-ink">Ways to earn</h2><p className="mt-1 text-sm text-slate-500">Every little action matters.</p></div><Zap className="text-amber-500" fill="currentColor" /></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[['Chat with CalmBot', '+5', MessageCircle], ['Write a journal entry', '+10', NotebookPen], ['Complete a check-in', '+5', Heart]].map(([label, amount, Icon]) => <div key={label} className="rounded-2xl bg-sky p-4"><Icon size={18} className="text-primary" /><p className="mt-4 text-xs font-bold leading-4 text-ink">{label}</p><p className="mt-2 text-sm font-extrabold text-primary">{amount} coins</p></div>)}</div></Card></div><Card className="mt-8 p-7"><div className="flex items-center justify-between"><h2 className="text-xl font-extrabold text-ink">Recent activity</h2><span className="text-xs font-bold text-slate-400">Last 20 actions</span></div><div className="mt-5 divide-y divide-slate-100">{transactions.map((transaction) => <div key={transaction._id || transaction.id} className="flex items-center gap-3 py-4"><span className={cx('grid h-10 w-10 place-items-center rounded-xl', transaction.transaction_type === 'spend' ? 'bg-coral text-rose-500' : 'bg-mint text-teal-700')}>{transaction.transaction_type === 'spend' ? <Zap size={17} /> : <Sparkles size={17} />}</span><div className="flex-1"><p className="text-sm font-bold text-ink">{transaction.description}</p><p className="mt-1 text-xs text-slate-400">{transaction.timestamp ? new Date(transaction.timestamp).toLocaleDateString() : 'Recently'}</p></div><span className={cx('text-sm font-extrabold', transaction.transaction_type === 'spend' ? 'text-rose-500' : 'text-teal-600')}>{transaction.transaction_type === 'spend' ? '-' : '+'}{transaction.amount}</span></div>)}</div></Card></>
}

function Protected({ children }) {
  return getStoredUser() ? <AppShell>{children}</AppShell> : <Navigate to="/login" replace />
}

export default function App() {
  return <Routes><Route path="/" element={<Landing />} /><Route path="/login" element={<AuthPage />} /><Route path="/register" element={<AuthPage mode="register" />} /><Route path="/dashboard" element={<Protected><Dashboard /></Protected>} /><Route path="/chat" element={<Protected><ChatPage /></Protected>} /><Route path="/journal" element={<Protected><JournalPage /></Protected>} /><Route path="/books" element={<Protected><BooksPage /></Protected>} /><Route path="/music" element={<Protected><MusicPage /></Protected>} /><Route path="/therapists" element={<Protected><TherapistsPage /></Protected>} /><Route path="/coins" element={<Protected><CoinsPage /></Protected>} /><Route path="*" element={<Navigate to="/" replace />} /></Routes>
}
