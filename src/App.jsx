import { useEffect, useState } from 'react'
import { Link, NavLink, Route, Routes, useLocation, useNavigate } from 'react-router-dom'
import {
  ArrowUpRight, BookOpen, Brain, Check, ChevronRight, CircleHelp, Coins, Headphones,
  Heart, LayoutDashboard, Leaf, LockKeyhole, Menu, MessageCircle, Moon, Play,
  Plus, Search, Send, ShieldCheck, Sparkles, Star, Users, Wallet, X, Zap,
} from 'lucide-react'
import { api, demoUser, withFallback } from './api'

const FALLBACK_THERAPISTS = [
  { _id: 'demo-sarah', name: 'Dr. Sarah Johnson', specializations: ['Anxiety', 'Mindfulness'], experience_years: 12, education: 'Ph.D. Clinical Psychology', bio: 'A warm, evidence-led approach to making space for the life you are building.', hourly_rate: 120, languages: ['English', 'Spanish'], rating: 4.8, total_sessions: 1247 },
  { _id: 'demo-maya', name: 'Maya Rodriguez, LMFT', specializations: ['Relationships', 'Self-esteem'], experience_years: 8, education: 'M.S. Marriage & Family Therapy', bio: 'Helping individuals and couples return to their own voice and create healthier connections.', hourly_rate: 100, languages: ['English', 'Spanish'], rating: 4.9, total_sessions: 654 },
  { _id: 'demo-aisha', name: 'Aisha Patel, LCSW', specializations: ['Grief & Loss', 'Life transitions'], experience_years: 7, education: 'MSW, University of Chicago', bio: 'Culturally sensitive support for the chapters that ask us to become someone new.', hourly_rate: 95, languages: ['English', 'Hindi', 'Gujarati'], rating: 4.8, total_sessions: 445 },
]
const FALLBACK_BOOKS = [
  { id: 'atomic', title: 'Atomic Habits', author: 'James Clear', description: 'Tiny changes, remarkable results — a practical guide to better systems.', image_url: 'https://images.unsplash.com/photo-1543002588-bfa74002ed7e?w=600&q=80' },
  { id: 'untethered', title: 'The Untethered Soul', author: 'Michael A. Singer', description: 'A journey beyond the thoughts and feelings that keep us stuck.', image_url: 'https://images.unsplash.com/photo-1512820790803-83ca734da794?w=600&q=80' },
  { id: 'comfort', title: 'The Comfort Book', author: 'Matt Haig', description: 'Notes, lists and stories for difficult days and quiet hope.', image_url: 'https://images.unsplash.com/photo-1495446815901-a7297e633e8d?w=600&q=80' },
]
const FALLBACK_JOURNALS = [
  { _id: 'j1', title: 'The day I chose softness', content: 'I took a walk without my phone today. The world felt less urgent.', mood: 'calm', created_at: '2026-09-04' },
  { _id: 'j2', title: 'A small win counts', content: 'Asked for help instead of pushing through alone.', mood: 'hopeful', created_at: '2026-09-02' },
]

function Button({ children, variant = 'primary', className = '', ...props }) {
  const styles = {
    primary: 'bg-lime text-ink hover:bg-white',
    secondary: 'bg-white/5 text-white border border-white/10 hover:bg-white/10',
    aqua: 'bg-aqua text-ink hover:bg-white',
    ghost: 'text-muted hover:text-white',
  }
  return <button className={`inline-flex items-center justify-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition ${styles[variant]} ${className}`} {...props}>{children}</button>
}

function Logo() {
  return <Link to="/" className="flex items-center gap-2.5 text-white"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-lime text-ink"><Leaf size={17} fill="currentColor" /></span><span className="display text-[17px] font-bold tracking-tight">zenheaven<span className="text-lime">.</span></span></Link>
}

const navItems = [['/dashboard', 'Dashboard', LayoutDashboard], ['/chat', 'Calm AI', MessageCircle], ['/journal', 'Journal', BookOpen], ['/books', 'Library', BookOpen], ['/music', 'Soundroom', Headphones], ['/therapists', 'Care team', Users], ['/coins', 'Vault', Coins]]

function Shell({ children, user, setUser }) {
  const [menu, setMenu] = useState(false)
  const location = useLocation()
  const isLanding = location.pathname === '/'
  if (isLanding) return <>{children}</>
  return <div className="min-h-screen bg-ink text-white"><header className="sticky top-0 z-30 border-b border-white/[.08] bg-ink/85 backdrop-blur-xl"><div className="mx-auto flex max-w-[1440px] items-center justify-between px-5 py-4 lg:px-10"><Logo /><nav className={`${menu ? 'absolute left-4 right-4 top-[70px] flex' : 'hidden'} glass flex-col gap-1 rounded-2xl p-2 lg:static lg:flex lg:flex-row lg:border-0 lg:bg-transparent lg:p-0`}>{navItems.map(([to, label, Icon]) => <NavLink key={to} to={to} onClick={() => setMenu(false)} className={({ isActive }) => `flex items-center gap-2 rounded-full px-3 py-2 text-xs font-medium transition ${isActive ? 'bg-lime text-ink' : 'text-muted hover:bg-white/5 hover:text-white'}`}><Icon size={14} />{label}</NavLink>)}</nav><div className="flex items-center gap-3"><Link to="/coins" className="hidden items-center gap-2 rounded-full border border-lime/20 bg-lime/10 px-3 py-2 text-xs text-lime sm:flex"><Coins size={14} />{user.calm_coins.toLocaleString()}</Link><button className="flex h-9 w-9 items-center justify-center rounded-full border border-white/10 bg-white/5 text-muted lg:hidden" onClick={() => setMenu(!menu)}>{menu ? <X size={17} /> : <Menu size={17} />}</button><button title="Sign out" onClick={() => { localStorage.removeItem('zen_token'); setUser(demoUser) }} className="hidden h-9 w-9 items-center justify-center rounded-full bg-aqua/15 text-aqua sm:flex"><span className="text-xs font-bold">{(user.full_name || 'M').slice(0, 1)}</span></button></div></div></header><main>{children}</main></div>
}

function Landing() {
  return <div className="noise min-h-screen overflow-hidden bg-ink"><header className="relative z-10 mx-auto flex max-w-[1440px] items-center justify-between px-5 py-6 lg:px-10"><Logo /><div className="hidden items-center gap-8 text-sm text-muted md:flex"><a href="#protocol">Protocol</a><a href="#rituals">Rituals</a><a href="#stories">Stories</a></div><div className="flex items-center gap-3"><Link to="/login" className="hidden px-3 py-2 text-sm text-muted hover:text-white sm:block">Sign in</Link><Link to="/register"><Button>Enter ZenHeaven <ArrowUpRight size={15} /></Button></Link></div></header><section className="grid-bg relative mx-auto grid max-w-[1440px] items-center gap-10 px-5 pb-24 pt-16 lg:grid-cols-[1.05fr_.95fr] lg:px-10 lg:pb-36 lg:pt-24"><div className="relative z-10"><p className="mb-7 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.24em] text-lime"><span className="h-px w-8 bg-lime" />A different kind of wealth</p><h1 className="display max-w-3xl text-5xl font-semibold leading-[.98] tracking-[-.05em] sm:text-7xl lg:text-[88px]">Make space<br />for your <span className="text-lime">better</span><br />days.</h1><p className="mt-8 max-w-md text-lg leading-8 text-muted">ZenHeaven is the calm layer for your life. Grow your wellbeing, one intentional ritual at a time.</p><div className="mt-10 flex flex-wrap gap-3"><Link to="/register"><Button className="px-6">Start your journey <ArrowUpRight size={16} /></Button></Link><a href="#protocol"><Button variant="secondary">See how it works</Button></a></div><div className="mt-16 flex items-center gap-5 text-xs text-muted"><div className="flex -space-x-2">{['#c7f36b', '#7ce5d3', '#f6c291', '#c8b4ff'].map((color, i) => <span key={color} className="h-8 w-8 rounded-full border-2 border-ink" style={{ backgroundColor: color }} />)}</div><span><strong className="text-white">12,400+</strong> people choosing softer days</span></div></div><div className="relative min-h-[500px] lg:min-h-[640px]"><div className="orb absolute left-1/2 top-1/2 h-[260px] w-[260px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-gradient-to-br from-lime via-[#9dcf70] to-aqua opacity-90 sm:h-[380px] sm:w-[380px]" /><div className="absolute left-[5%] top-[12%] w-48 rotate-[-7deg] rounded-2xl border border-white/10 bg-ink/80 p-4 shadow-glow backdrop-blur-lg"><div className="mb-5 flex justify-between text-xs text-muted"><span>YOUR BALANCE</span><Wallet size={14} className="text-lime" /></div><p className="display text-3xl font-semibold">$2,840<span className="text-sm text-muted">.42</span></p><div className="mt-4 h-1 rounded bg-white/10"><div className="h-1 w-3/4 rounded bg-lime" /></div><p className="mt-2 text-[10px] text-lime">+12.4% this month</p></div><div className="absolute bottom-[11%] right-[1%] w-52 rotate-[8deg] rounded-2xl bg-[#f3eee5] p-5 text-ink shadow-2xl"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-black/40">Today’s yield</p><p className="display mt-3 text-4xl font-semibold">+ 18 <span className="text-sm">calm</span></p><div className="mt-4 flex items-center gap-2 text-xs font-semibold"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-ink text-lime"><Check size={13} /></span>Morning ritual complete</div></div><div className="glass absolute bottom-[31%] left-[8%] rounded-full px-4 py-3 text-xs text-aqua"><span className="mr-2 inline-block h-2 w-2 rounded-full bg-aqua" />Your calm is compounding</div><div className="absolute right-[12%] top-[18%] flex h-20 w-20 items-center justify-center rounded-full border border-white/20 bg-ink/50 backdrop-blur-lg"><Sparkles className="text-lime" size={26} /></div></div></section><section id="protocol" className="mx-auto max-w-[1440px] px-5 py-24 lg:px-10"><div className="grid gap-14 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-xs font-semibold uppercase tracking-[.24em] text-aqua">The protocol</p><h2 className="display mt-5 max-w-md text-4xl font-semibold leading-tight tracking-[-.04em] sm:text-5xl">Your life is the asset.</h2></div><div className="grid gap-4 sm:grid-cols-3">{[['01', 'Deposit attention', 'Small rituals create the capital your future self can spend.'], ['02', 'Let it compound', 'A little reflection, connection and care add up faster than you think.'], ['03', 'Withdraw peace', 'Access the tools, people and perspective you need — right when you need them.']].map(([num, title, text]) => <div key={num} className="border-t border-white/15 pt-5"><span className="text-xs text-lime">{num}</span><h3 className="display mt-10 text-xl font-semibold">{title}</h3><p className="mt-3 text-sm leading-6 text-muted">{text}</p></div>)}</div></div></section><section id="rituals" className="border-y border-white/[.08] bg-[#0d1524] px-5 py-24 lg:px-10"><div className="mx-auto max-w-[1440px]"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.24em] text-lime">One home for all of you</p><h2 className="display mt-4 max-w-xl text-4xl font-semibold tracking-[-.04em] sm:text-5xl">Build a portfolio<br />of feeling good.</h2></div><Link to="/register" className="flex items-center gap-2 text-sm text-lime">Explore the rituals <ArrowUpRight size={15} /></Link></div><div className="mt-14 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[['Calm AI', MessageCircle, 'A conversation that meets you where you are.', 'bg-lime text-ink'], ['Journal', BookOpen, 'Turn the noise in your head into a signal.', 'bg-aqua text-ink'], ['Soundroom', Headphones, 'A soundtrack for the state you want to be in.', 'bg-[#c8b4ff] text-ink'], ['Care team', Users, 'Real humans, ready when you need a hand.', 'bg-[#f6c291] text-ink']].map(([title, Icon, text, color]) => <Link to={`/${title === 'Calm AI' ? 'chat' : title === 'Care team' ? 'therapists' : title.toLowerCase()}`} key={title} className="group rounded-2xl border border-white/10 bg-ink p-6 transition hover:-translate-y-1 hover:border-white/25"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${color}`}><Icon size={19} /></div><h3 className="display mt-16 text-xl font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{text}</p><ChevronRight className="mt-7 text-muted transition group-hover:translate-x-1 group-hover:text-white" size={18} /></Link>)}</div></div></section><section id="stories" className="mx-auto max-w-[1440px] px-5 py-24 lg:px-10"><div className="grid items-center gap-14 lg:grid-cols-2"><div className="relative h-[390px] rounded-3xl bg-gradient-to-br from-[#223d43] via-[#10262e] to-ink p-8"><div className="absolute right-8 top-8 h-36 w-36 rounded-full border border-aqua/30 bg-aqua/10" /><div className="absolute right-14 top-14 h-24 w-24 rounded-full border border-aqua/40 bg-aqua/10" /><div className="absolute bottom-8 left-8"><p className="max-w-xs text-2xl leading-tight text-white">“I stopped waiting to feel better and started building better.”</p><p className="mt-5 text-xs uppercase tracking-[.2em] text-aqua">— Noor, member since 2024</p></div></div><div><p className="text-xs font-semibold uppercase tracking-[.24em] text-aqua">The long game</p><h2 className="display mt-5 text-4xl font-semibold leading-tight tracking-[-.04em] sm:text-5xl">There’s no perfect<br />version of you.</h2><p className="mt-6 max-w-md text-base leading-7 text-muted">Just the next honest moment. ZenHeaven gives you a private, beautiful place to notice it, nurture it and keep going.</p><Link to="/register" className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-lime">Open your vault <ArrowUpRight size={15} /></Link></div></div></section><footer className="border-t border-white/[.08] px-5 py-8 lg:px-10"><div className="mx-auto flex max-w-[1440px] flex-wrap items-center justify-between gap-4 text-xs text-muted"><Logo /><span>© 2026 ZenHeaven. A softer internet.</span><span className="flex items-center gap-2"><ShieldCheck size={14} className="text-lime" />Private by design</span></div></footer></div>
}

function PageHeader({ eyebrow, title, description, action }) {
  return <div className="border-b border-white/[.08] bg-[#0d1524] px-5 py-12 lg:px-10"><div className="mx-auto flex max-w-[1240px] flex-wrap items-end justify-between gap-8"><div><p className="text-xs font-semibold uppercase tracking-[.22em] text-lime">{eyebrow}</p><h1 className="display mt-4 text-4xl font-semibold tracking-[-.04em] sm:text-5xl">{title}</h1>{description && <p className="mt-4 max-w-xl text-sm leading-6 text-muted">{description}</p>}</div>{action}</div></div>
}

function Dashboard({ user }) {
  const [balance, setBalance] = useState(user.calm_coins)
  const [goals, setGoals] = useState([])
  useEffect(() => { withFallback(api.balance, { balance: user.calm_coins }).then((result) => setBalance(result.balance)); withFallback(api.goals, []).then(setGoals) }, [user.calm_coins])
  const displayGoals = goals.length ? goals : [{ title: 'Morning check-in', current: 1, target: 1, coins: 10, completed: true }, { title: 'Write in your journal', current: 0, target: 1, coins: 15 }, { title: 'Take a mindful pause', current: 0, target: 1, coins: 5 }]
  return <><PageHeader eyebrow="Your personal vault" title={`Good evening, ${user.full_name || 'friend'}.`} description="Your wellbeing is not a balance to optimize. It’s a relationship to tend." action={<Button variant="secondary"><CircleHelp size={15} />How it works</Button>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="grid gap-5 lg:grid-cols-[1.5fr_1fr]"><div className="relative overflow-hidden rounded-3xl bg-lime p-7 text-ink sm:p-10"><div className="absolute -right-20 -top-20 h-64 w-64 rounded-full border-[40px] border-ink/5" /><div className="relative flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-ink/60">Calm balance</p><p className="display mt-6 text-6xl font-semibold tracking-[-.06em]">{balance.toLocaleString()}<span className="text-xl"> CC</span></p><p className="mt-3 flex items-center gap-1 text-sm text-ink/70"><ArrowUpRight size={14} /> 8.2% since last month</p></div><div className="rounded-full bg-ink/10 p-3"><Coins size={21} /></div></div><div className="relative mt-16 flex items-end justify-between border-t border-ink/15 pt-5"><div><p className="text-xs text-ink/60">Next milestone</p><p className="mt-1 text-sm font-semibold">The steady self · 3,000 CC</p></div><div className="text-right"><p className="text-xs text-ink/60">160 to go</p><div className="mt-2 h-1.5 w-32 rounded-full bg-ink/15"><div className="h-1.5 w-[94%] rounded-full bg-ink" /></div></div></div></div><div className="glass rounded-3xl p-7"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-muted">Your streak</p><p className="display mt-4 text-5xl font-semibold">06 <span className="text-lg text-muted">days</span></p></div><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-aqua/15 text-aqua"><Zap size={25} fill="currentColor" /></div></div><div className="mt-7 flex justify-between gap-2">{['M', 'T', 'W', 'T', 'F', 'S', 'S'].map((day, i) => <div key={`${day}-${i}`} className="text-center"><div className={`flex h-8 w-8 items-center justify-center rounded-full text-[10px] ${i < 6 ? 'bg-aqua text-ink' : 'border border-white/10 text-muted'}`}>{i < 6 ? <Check size={13} /> : '·'}</div><span className="mt-2 block text-[10px] text-muted">{day}</span></div>)}</div><p className="mt-6 text-xs leading-5 text-muted">One more day to unlock your <span className="text-aqua">50 CC</span> weekly bonus.</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_1.5fr]"><div className="glass rounded-3xl p-7"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-muted">Today’s rituals</p><h2 className="display mt-2 text-2xl font-semibold">Keep the flow</h2></div><span className="text-xs text-lime">{displayGoals.filter((g) => g.completed).length}/{displayGoals.length}</span></div><div className="mt-7 space-y-5">{displayGoals.map((goal, i) => <div key={goal.title || i} className="flex items-center gap-3"><span className={`flex h-8 w-8 items-center justify-center rounded-full ${goal.completed ? 'bg-lime text-ink' : 'border border-white/15 text-muted'}`}>{goal.completed ? <Check size={14} /> : <span className="text-xs">{i + 1}</span>}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-3 text-sm"><span className={goal.completed ? 'text-muted line-through' : 'text-white'}>{goal.title}</span><span className="text-xs text-lime">+{goal.coins} CC</span></div><div className="mt-2 h-1 rounded bg-white/10"><div className="h-1 rounded bg-aqua" style={{ width: `${goal.completed ? 100 : ((goal.current || 0) / (goal.target || 1)) * 100}%` }} /></div></div></div>)}</div><Link to="/journal" className="mt-7 flex items-center gap-1 text-xs font-semibold text-lime">View all rituals <ChevronRight size={14} /></Link></div><div className="glass rounded-3xl p-7"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-muted">A note from Calm AI</p><h2 className="display mt-2 text-2xl font-semibold">You’ve been showing up.</h2></div><div className="flex h-10 w-10 items-center justify-center rounded-full bg-lime text-ink"><Sparkles size={18} /></div></div><div className="mt-8 rounded-2xl bg-white/[.04] p-5"><p className="text-lg leading-8 text-white/90">“Not every day needs a breakthrough. Some days, the win is noticing that you made it here — and that can be enough.”</p><p className="mt-4 text-xs text-muted">Based on your last 7 check-ins · <Link to="/chat" className="text-aqua">Talk it through</Link></p></div><div className="mt-6 flex flex-wrap gap-2 text-xs text-muted"><span className="rounded-full border border-white/10 px-3 py-1.5">rest</span><span className="rounded-full border border-white/10 px-3 py-1.5">self-trust</span><span className="rounded-full border border-white/10 px-3 py-1.5">momentum</span></div></div></div></div></>
}

function Auth({ mode, setUser }) {
  const navigate = useNavigate()
  const [form, setForm] = useState({ username: '', email: '', password: '', full_name: '' })
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)
  const isRegister = mode === 'register'
  async function submit(event) { event.preventDefault(); setLoading(true); setError(''); try { const result = await (isRegister ? api.register(form) : api.login({ username: form.username, password: form.password })); setUser(result); navigate('/dashboard') } catch (requestError) { setError(`${requestError.message}. You can continue in demo mode.`); setUser({ ...demoUser, full_name: form.full_name || form.username || 'friend' }); navigate('/dashboard') } finally { setLoading(false) } }
  return <div className="grid min-h-screen lg:grid-cols-2"><div className="noise grid-bg hidden flex-col justify-between bg-[#101d2a] p-10 lg:flex"><Logo /><div><p className="mb-6 text-xs font-semibold uppercase tracking-[.22em] text-lime">Welcome to the softer side</p><h1 className="display max-w-lg text-6xl font-semibold leading-[.95] tracking-[-.05em]">Your future self is already proud.</h1><p className="mt-7 max-w-sm leading-7 text-muted">A private space for your rituals, reflections and the support you deserve.</p></div><p className="text-xs text-muted">Private by design · Human when it matters</p></div><div className="flex flex-col justify-center px-6 py-10 sm:px-16"><div className="mb-12 flex items-center justify-between lg:justify-end"><div className="lg:hidden"><Logo /></div><Link to="/" className="text-sm text-muted hover:text-white">Back to home</Link></div><div className="mx-auto w-full max-w-md"><p className="text-xs font-semibold uppercase tracking-[.22em] text-lime">{isRegister ? 'Open your vault' : 'Welcome back'}</p><h1 className="display mt-4 text-4xl font-semibold tracking-[-.04em]">{isRegister ? 'Start where you are.' : 'Good to see you.'}</h1><p className="mt-4 text-sm leading-6 text-muted">{isRegister ? 'Your calm balance starts with one small decision.' : 'Your space is ready when you are.'}</p><form onSubmit={submit} className="mt-10 space-y-4">{isRegister && <label className="block text-xs text-muted">What should we call you?<input required value={form.full_name} onChange={(e) => setForm({ ...form, full_name: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm text-white outline-none focus:border-lime" placeholder="Your first name" /></label>}<label className="block text-xs text-muted">Username<input required value={form.username} onChange={(e) => setForm({ ...form, username: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm text-white outline-none focus:border-lime" placeholder="e.g. moonwalker" /></label>{isRegister && <label className="block text-xs text-muted">Email<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm text-white outline-none focus:border-lime" placeholder="you@example.com" /></label>}<label className="block text-xs text-muted">Password<input required type="password" minLength="6" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.04] px-4 py-3 text-sm text-white outline-none focus:border-lime" placeholder="••••••••" /></label>{error && <p className="rounded-xl border border-lime/20 bg-lime/10 p-3 text-xs text-lime">{error}</p>}<Button disabled={loading} className="mt-3 w-full">{loading ? 'Opening…' : isRegister ? 'Create my space' : 'Enter ZenHeaven'} <ArrowUpRight size={15} /></Button></form><p className="mt-8 text-center text-sm text-muted">{isRegister ? 'Already have a space?' : 'New here?'} <Link to={isRegister ? '/login' : '/register'} className="text-lime">{isRegister ? 'Sign in' : 'Create an account'}</Link></p></div></div></div>
}

function Chat() {
  const [messages, setMessages] = useState([{ role: 'assistant', text: 'Hey, I’m here. What’s taking up the most space in your mind today?' }])
  const [input, setInput] = useState('')
  const [sending, setSending] = useState(false)
  const [threadId, setThreadId] = useState(null)
  async function send(event) { event.preventDefault(); if (!input.trim() || sending) return; const message = input.trim(); setInput(''); setMessages((items) => [...items, { role: 'user', text: message }, { role: 'assistant', text: '' }]); setSending(true); let response = ''; try { await api.chat(message, threadId, (eventData) => { if (eventData.type === 'thread_id') setThreadId(eventData.data); if (eventData.type === 'token') { response += eventData.data; setMessages((items) => items.map((item, index) => index === items.length - 1 ? { ...item, text: response } : item)) } }); if (!response) throw new Error('empty') } catch { response = 'I’m with you. Try taking one slow breath, then name the feeling without needing to solve it yet. What would feel like a kind next step?'; setMessages((items) => items.map((item, index) => index === items.length - 1 ? { ...item, text: response } : item)) } finally { setSending(false) } }
  return <div className="mx-auto grid max-w-[1240px] gap-5 px-5 py-8 lg:grid-cols-[280px_1fr] lg:px-10"><aside className="glass hidden rounded-3xl p-5 lg:block"><div className="flex items-center justify-between"><p className="text-xs uppercase tracking-[.18em] text-muted">Your threads</p><button className="text-muted hover:text-white" onClick={() => setMessages([{ role: 'assistant', text: 'A fresh page. What’s here for you today?' }])}><Plus size={16} /></button></div><div className="mt-6 rounded-2xl bg-lime/10 p-4"><p className="text-sm font-medium text-lime">A softer morning</p><p className="mt-2 text-xs text-muted">Today · 8 messages</p></div><p className="mt-7 text-xs leading-5 text-muted">Your conversations are private and never used to judge your progress.</p><div className="mt-8 flex items-center gap-2 text-xs text-aqua"><LockKeyhole size={13} />End-to-end private space</div></aside><section className="glass flex min-h-[calc(100vh-132px)] flex-col overflow-hidden rounded-3xl"><div className="flex items-center justify-between border-b border-white/[.08] px-5 py-4 sm:px-7"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-lime text-ink"><Brain size={17} /></div><div><p className="text-sm font-semibold">Calm AI</p><p className="flex items-center gap-1 text-[11px] text-aqua"><span className="h-1.5 w-1.5 rounded-full bg-aqua" />Present with you</p></div></div><Button variant="secondary" className="hidden px-3 py-2 text-xs sm:inline-flex"><CircleHelp size={13} />Crisis support</Button></div><div className="flex-1 space-y-6 overflow-y-auto p-5 sm:p-10">{messages.map((message, index) => <div key={`${message.role}-${index}`} className={`flex max-w-2xl gap-3 ${message.role === 'user' ? 'ml-auto flex-row-reverse' : ''}`}><div className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-full ${message.role === 'user' ? 'bg-aqua text-ink' : 'bg-lime text-ink'}`}>{message.role === 'user' ? <span className="text-xs font-bold">M</span> : <Sparkles size={14} />}</div><div className={`rounded-2xl px-4 py-3 text-sm leading-7 ${message.role === 'user' ? 'bg-aqua/10 text-white' : 'bg-white/[.05] text-white/85'}`}>{message.text || <span className="inline-flex gap-1"><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-lime" /><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-lime [animation-delay:150ms]" /><i className="h-1.5 w-1.5 animate-bounce rounded-full bg-lime [animation-delay:300ms]" /></span>}</div></div>)}</div><form onSubmit={send} className="border-t border-white/[.08] p-4 sm:p-6"><div className="flex items-center gap-2 rounded-2xl border border-white/10 bg-white/[.03] px-4 py-2 focus-within:border-lime/50"><input value={input} onChange={(e) => setInput(e.target.value)} placeholder="Say what’s on your mind…" className="min-w-0 flex-1 bg-transparent py-2 text-sm text-white outline-none placeholder:text-muted" /><button className="flex h-9 w-9 items-center justify-center rounded-full bg-lime text-ink transition hover:bg-white" title="Send"><Send size={15} /></button></div><p className="mt-3 text-center text-[10px] text-muted">Calm AI is a supportive companion, not a replacement for professional care.</p></form></section></div>
}

function Journal() {
  const [entries, setEntries] = useState([])
  const [content, setContent] = useState('')
  const [mood, setMood] = useState('calm')
  const [saved, setSaved] = useState(false)
  useEffect(() => { withFallback(api.journal, FALLBACK_JOURNALS).then((items) => setEntries(Array.isArray(items) ? items : FALLBACK_JOURNALS)) }, [])
  async function save(event) { event.preventDefault(); if (!content.trim()) return; const entry = { content, mood, tags: [] }; const result = await withFallback(() => api.createJournal(entry), { ...entry, _id: `local-${Date.now()}`, title: 'A note to myself', created_at: new Date().toISOString() }); setEntries((items) => [result, ...items]); setContent(''); setSaved(true); setTimeout(() => setSaved(false), 2500) }
  return <><PageHeader eyebrow="Deposit attention" title="The journal" description="A private place to turn what you feel into something you can hold." action={<div className="flex items-center gap-2 text-xs text-muted"><span className="h-2 w-2 rounded-full bg-aqua" />Saved privately</div>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><form onSubmit={save} className="rounded-3xl bg-[#f1ede4] p-7 text-ink sm:p-10"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.18em] text-black/40">New reflection</p><span className="text-xs text-black/40">+10 CC</span></div><h2 className="display mt-10 text-3xl font-semibold">What feels true today?</h2><textarea value={content} onChange={(e) => setContent(e.target.value)} className="mt-6 min-h-44 w-full resize-none bg-transparent text-lg leading-8 outline-none placeholder:text-black/30" placeholder="No need to make it beautiful…" /><div className="mt-7 flex flex-wrap items-center gap-2 border-t border-black/10 pt-5"><span className="mr-2 text-xs text-black/50">I feel</span>{['calm', 'hopeful', 'anxious', 'tired'].map((item) => <button type="button" key={item} onClick={() => setMood(item)} className={`rounded-full px-3 py-1.5 text-xs transition ${mood === item ? 'bg-ink text-lime' : 'bg-black/5 text-black/60 hover:bg-black/10'}`}>{item}</button>)}<Button className="ml-auto px-4 py-2.5">{saved ? <Check size={15} /> : 'Save entry'}</Button></div></form><div className="glass rounded-3xl p-7"><p className="text-xs uppercase tracking-[.18em] text-muted">A prompt for you</p><p className="display mt-8 text-2xl leading-9">“What would you do today if you trusted that you were already enough?”</p><button className="mt-8 flex items-center gap-2 text-xs text-lime" onClick={() => setContent('Today, I trusted that I was already enough when…')}>Use this prompt <ArrowUpRight size={14} /></button><div className="mt-12 border-t border-white/10 pt-5"><p className="text-xs text-muted">Your mood, lately</p><div className="mt-4 flex h-20 items-end gap-2">{[35, 52, 44, 68, 56, 74, 62, 82, 76, 88, 73, 90].map((height, i) => <div key={i} className="flex-1 rounded-t bg-gradient-to-t from-aqua/40 to-lime" style={{ height: `${height}%` }} />)}</div><div className="mt-2 flex justify-between text-[10px] text-muted"><span>12 days ago</span><span>today</span></div></div></div></div><div className="mt-14"><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-muted">Your archive</p><h2 className="display mt-2 text-2xl font-semibold">Small truths</h2></div><span className="text-xs text-muted">{entries.length} entries</span></div><div className="mt-6 grid gap-3 md:grid-cols-2">{entries.map((entry, i) => <article key={entry._id || entry.id || i} className="glass rounded-2xl p-5"><div className="flex items-start justify-between gap-4"><div><h3 className="font-semibold">{entry.title || 'A reflection'}</h3><p className="mt-1 text-xs text-muted">{entry.created_at ? new Date(entry.created_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' }) : 'Recently'}</p></div><span className="rounded-full bg-aqua/10 px-2.5 py-1 text-[10px] text-aqua">{entry.mood || 'reflective'}</span></div><p className="mt-5 line-clamp-2 text-sm leading-6 text-muted">{entry.content}</p></article>)}</div></div></div></>
}

function Books() {
  const [books, setBooks] = useState(FALLBACK_BOOKS)
  const [search, setSearch] = useState('')
  useEffect(() => { withFallback(() => api.books(), { books: FALLBACK_BOOKS }).then((result) => { if (result.books?.length) setBooks(result.books) }) }, [])
  const filtered = books.filter((book) => `${book.title} ${book.author}`.toLowerCase().includes(search.toLowerCase()))
  return <><PageHeader eyebrow="Withdraw perspective" title="The library" description="Books selected for the chapter you’re in — because sometimes another voice knows the way." action={<div className="relative"><Search className="absolute left-3 top-3 text-muted" size={15} /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search the library" className="w-52 rounded-full border border-white/10 bg-white/5 py-2.5 pl-9 pr-3 text-xs outline-none focus:border-lime sm:w-64" /></div>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="mb-10 rounded-3xl bg-gradient-to-r from-[#1e3942] to-[#13202c] p-7 sm:p-10"><p className="text-xs uppercase tracking-[.18em] text-aqua">Picked for your recent mood</p><div className="mt-5 flex flex-wrap items-end justify-between gap-5"><div><h2 className="display text-3xl font-semibold">Books for finding your center</h2><p className="mt-2 text-sm text-muted">A little calm, a little clarity, right when you need it.</p></div><Button variant="aqua">Refresh picks <Sparkles size={15} /></Button></div></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((book, i) => <article key={book.id || i} className="group overflow-hidden rounded-2xl border border-white/10 bg-[#101a29]"><div className="relative h-56 overflow-hidden bg-[#233247]"><img src={book.image_url} alt="" className="h-full w-full object-cover opacity-80 transition duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#101a29] to-transparent" /><span className="absolute left-4 top-4 rounded-full bg-ink/75 px-2.5 py-1 text-[10px] text-lime">{i === 0 ? 'Top pick' : 'For you'}</span></div><div className="p-5"><h3 className="display text-xl font-semibold">{book.title}</h3><p className="mt-1 text-xs text-aqua">{book.author}</p><p className="mt-4 line-clamp-2 text-sm leading-6 text-muted">{book.description}</p><button className="mt-5 flex items-center gap-1 text-xs font-semibold text-lime">Read the feeling <ArrowUpRight size={13} /></button></div></article>)}</div></div></>
}

function Music() {
  const tracks = [['Weightless', 'Marconi Union', '00:08:02'], ['Bloom', 'The Paper Kites', '00:03:30'], ['Anchor', 'Novo Amor', '00:03:30'], ['Sunset Lover', 'Petit Biscuit', '00:03:58']]
  const [playing, setPlaying] = useState(0)
  return <><PageHeader eyebrow="Regulate your rhythm" title="The soundroom" description="A soundtrack for where you are — or where you want to gently arrive." action={<Button variant="secondary"><Heart size={15} />Save mix</Button>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="grid gap-5 lg:grid-cols-[.85fr_1.15fr]"><div className="relative min-h-[410px] overflow-hidden rounded-3xl bg-gradient-to-br from-[#c8b4ff] via-[#7c87da] to-[#223247] p-7 text-ink"><div className="absolute -right-16 -top-12 h-64 w-64 rounded-full border-[34px] border-white/20" /><div className="absolute bottom-8 left-7"><p className="text-xs font-bold uppercase tracking-[.2em] text-ink/60">Curated for you</p><h2 className="display mt-3 text-4xl font-semibold">Soft landing</h2><p className="mt-2 text-sm text-ink/70">A 32-minute descent into stillness.</p><button onClick={() => setPlaying(playing === 0 ? -1 : 0)} className="mt-8 flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-sm font-semibold text-white"><Play size={15} fill="currentColor" />{playing === 0 ? 'Playing now' : 'Play mix'}</button></div></div><div className="glass rounded-3xl p-7"><div className="flex items-center justify-between"><p className="text-xs uppercase tracking-[.18em] text-muted">Now playing</p><span className="flex items-center gap-1 text-[10px] text-aqua"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-aqua" />Spatial audio</span></div><div className="mt-8 flex items-center gap-5"><div className="flex h-20 w-20 items-center justify-center rounded-2xl bg-gradient-to-br from-lime to-aqua text-ink"><Moon size={30} /></div><div><h3 className="display text-2xl font-semibold">{tracks[playing >= 0 ? playing : 0][0]}</h3><p className="mt-1 text-sm text-muted">{tracks[playing >= 0 ? playing : 0][1]}</p></div></div><div className="mt-10 h-1 rounded bg-white/10"><div className="relative h-1 w-[42%] rounded bg-aqua"><span className="absolute -right-1 -top-1 h-3 w-3 rounded-full bg-white" /></div></div><div className="mt-2 flex justify-between text-[10px] text-muted"><span>02:14</span><span>08:02</span></div><div className="mt-8 space-y-1">{tracks.map((track, i) => <button onClick={() => setPlaying(i)} key={track[0]} className={`flex w-full items-center gap-4 rounded-xl p-3 text-left transition ${playing === i ? 'bg-lime/10' : 'hover:bg-white/5'}`}><span className={`flex h-7 w-7 items-center justify-center rounded-full ${playing === i ? 'bg-lime text-ink' : 'bg-white/10 text-muted'}`}>{playing === i ? <span className="text-[10px]">Ⅱ</span> : <Play size={11} fill="currentColor" />}</span><span className="flex-1 text-sm">{track[0]}<small className="ml-2 text-xs text-muted">{track[1]}</small></span><span className="text-xs text-muted">{track[2]}</span></button>)}</div></div></div></div></>
}

function Therapists() {
  const [therapists, setTherapists] = useState(FALLBACK_THERAPISTS)
  useEffect(() => { withFallback(api.therapists, FALLBACK_THERAPISTS).then((result) => setTherapists(result.length ? result : FALLBACK_THERAPISTS)) }, [])
  return <><PageHeader eyebrow="Human when it matters" title="Your care team" description="Licensed professionals who meet you with skill, warmth and zero judgment." action={<Button><Search size={15} />Find your match</Button>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="mb-8 flex flex-wrap gap-2">{['All specialties', 'Anxiety', 'Relationships', 'Life transitions', 'Available this week'].map((filter, i) => <button key={filter} className={`rounded-full border px-4 py-2 text-xs ${i === 0 ? 'border-lime bg-lime text-ink' : 'border-white/10 text-muted hover:border-white/30 hover:text-white'}`}>{filter}</button>)}</div><div className="grid gap-5 lg:grid-cols-3">{therapists.map((person, i) => <article key={person._id || i} className="glass rounded-3xl p-6"><div className="flex items-start gap-4"><div className={`flex h-16 w-16 shrink-0 items-center justify-center rounded-2xl text-xl font-semibold text-ink ${['bg-lime', 'bg-aqua', 'bg-[#f6c291]'][i % 3]}`}>{person.name.split(' ').map((part) => part[0]).slice(0, 2).join('')}</div><div><h3 className="display text-lg font-semibold">{person.name}</h3><p className="mt-1 flex items-center gap-1 text-xs text-lime"><Star size={12} fill="currentColor" />{person.rating} <span className="text-muted">· {person.total_sessions} sessions</span></p><p className="mt-2 text-xs text-muted">{person.experience_years} years experience</p></div></div><p className="mt-6 text-sm leading-6 text-muted">{person.bio}</p><div className="mt-5 flex flex-wrap gap-1.5">{person.specializations.slice(0, 3).map((specialty) => <span key={specialty} className="rounded-full bg-white/5 px-2.5 py-1 text-[10px] text-muted">{specialty}</span>)}</div><div className="mt-6 flex items-center justify-between border-t border-white/10 pt-5"><span className="text-xs text-muted"><strong className="text-white">${person.hourly_rate}</strong> / session</span><Button variant="secondary" className="px-4 py-2.5 text-xs">View profile <ChevronRight size={13} /></Button></div></article>)}</div></div></>
}

function CoinsPage({ user }) {
  const [transactions, setTransactions] = useState([])
  useEffect(() => { withFallback(api.transactions, [{ amount: 15, source: 'Journal entry', transaction_type: 'earn', timestamp: '2026-09-05' }, { amount: 5, source: 'Calm AI chat', transaction_type: 'earn', timestamp: '2026-09-04' }, { amount: 100, source: 'Premium insights', transaction_type: 'spend', timestamp: '2026-09-01' }]).then(setTransactions) }, [])
  return <><PageHeader eyebrow="Your wellbeing, made visible" title="The calm vault" description="Calm Coins celebrate the care you put back into yourself. They never expire." action={<Button><Plus size={15} />Earn more</Button>} /><div className="mx-auto max-w-[1240px] px-5 py-10 lg:px-10"><div className="grid gap-5 md:grid-cols-3"><div className="rounded-3xl bg-lime p-7 text-ink md:col-span-2"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-ink/60">Available calm</p><p className="display mt-5 text-6xl font-semibold tracking-[-.06em]">{user.calm_coins.toLocaleString()}<span className="text-2xl"> CC</span></p></div><Coins size={25} /></div><div className="mt-14 flex flex-wrap gap-3"><span className="rounded-full bg-ink/10 px-3 py-2 text-xs">+10 for every journal</span><span className="rounded-full bg-ink/10 px-3 py-2 text-xs">+5 for every check-in</span></div></div><div className="glass rounded-3xl p-7"><p className="text-xs uppercase tracking-[.18em] text-muted">Your impact</p><p className="display mt-4 text-4xl font-semibold">18.4<span className="text-base text-muted"> hrs</span></p><p className="mt-2 text-sm text-muted">invested in you this month</p><div className="mt-8 flex items-end gap-1">{[30, 42, 35, 55, 48, 70, 82].map((h, i) => <div key={i} className="flex-1 rounded-t bg-aqua/70" style={{ height: `${h}px` }} />)}</div></div></div><div className="mt-12 grid gap-8 lg:grid-cols-[1fr_.8fr]"><div><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-muted">Recent movement</p><h2 className="display mt-2 text-2xl font-semibold">Your trail</h2></div><button className="text-xs text-lime">See all</button></div><div className="mt-6 overflow-hidden rounded-2xl border border-white/10">{transactions.map((transaction, i) => <div key={`${transaction.source}-${i}`} className="flex items-center gap-4 border-b border-white/[.08] p-4 last:border-0"><div className={`flex h-9 w-9 items-center justify-center rounded-full ${transaction.transaction_type === 'spend' ? 'bg-white/10 text-muted' : 'bg-lime/15 text-lime'}`}>{transaction.transaction_type === 'spend' ? <ArrowUpRight size={15} /> : <Plus size={15} />}</div><div className="flex-1"><p className="text-sm">{transaction.source}</p><p className="mt-1 text-[11px] text-muted">{transaction.timestamp?.slice(0, 10)}</p></div><span className={`text-sm font-semibold ${transaction.transaction_type === 'spend' ? 'text-white' : 'text-lime'}`}>{transaction.transaction_type === 'spend' ? '-' : '+'}{transaction.amount} CC</span></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-[#101a29] p-7"><p className="text-xs uppercase tracking-[.18em] text-aqua">Ways to grow</p><h2 className="display mt-3 text-2xl font-semibold">The compounding list</h2><div className="mt-7 space-y-5">{[['Write a reflection', '+10 CC', BookOpen], ['Talk to Calm AI', '+5 CC', MessageCircle], ['Book a care session', 'from 500 CC', Users]].map(([title, amount, Icon]) => <div key={title} className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-white/5 text-aqua"><Icon size={15} /></div><span className="flex-1 text-sm">{title}</span><span className="text-xs text-lime">{amount}</span></div>)}</div></div></div></div></>
}

function App() {
  const [user, setUser] = useState(demoUser)
  useEffect(() => { if (localStorage.getItem('zen_token')) withFallback(api.me, null).then((result) => { if (result) setUser(result) }) }, [])
  return <Shell user={user} setUser={setUser}><Routes><Route path="/" element={<Landing />} /><Route path="/login" element={<Auth mode="login" setUser={setUser} />} /><Route path="/register" element={<Auth mode="register" setUser={setUser} />} /><Route path="/dashboard" element={<Dashboard user={user} />} /><Route path="/chat" element={<><PageHeader eyebrow="A little less alone" title="Calm AI" description="A private conversation for the thoughts that don’t fit anywhere else." /><Chat /></>} /><Route path="/journal" element={<Journal />} /><Route path="/books" element={<Books />} /><Route path="/music" element={<Music />} /><Route path="/therapists" element={<Therapists />} /><Route path="/coins" element={<CoinsPage user={user} />} /><Route path="*" element={<Dashboard user={user} />} /></Routes></Shell>
}

export default App
